<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dell.indianmistry">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="Sign In">

        </activity>
        <activity
            android:name=".SignUp"
            android:label="Sign Up" />
        <activity
            android:name=".otp"
            android:label="OTP" />
        <activity
            android:name=".logged_in_user"
            android:label="Indian Mistry"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".profile"
            android:label="Profile" />
        <activity
            android:name=".service"
            android:label="Services" />
        <activity
            android:name=".schedule_service"
            android:label="Schedule Your Service" />
        <activity
            android:name=".book_service"
            android:label="Book Service" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyABX7_pvX8EB5Po6xcSfCCFPWinn2kO0ag" />

        <activity android:name=".splashscreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>